services:
  backend:
    build: ./backend
    ports: ["8000:8000"]
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
  frontend:
    build: ./frontend
    ports: ["5173:5173"]
    depends_on: [backend]
